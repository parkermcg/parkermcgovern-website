# Lead delivery — see CLAUDE.md §9
# Resend API key (https://resend.com). Without it the form returns a handled error.
RESEND_API_KEY=
# Verified sender on parkermcgovern.ca, e.g. "Website <leads@parkermcgovern.ca>"
LEAD_FROM=
LEAD_TO=parker@mortgagebroker.ca
# Durable backstop: any endpoint accepting POST JSON (Google Apps Script, Zapier, Make)
LEAD_WEBHOOK_URL=

# Analytics — deferred, see CLAUDE.md §10. Wiring is inert until set.
NEXT_PUBLIC_GA4_ID=
